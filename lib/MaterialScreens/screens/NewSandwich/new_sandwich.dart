import 'package:flutter/material.dart';

class NewSandwich extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('New Sandwich'),
    );
  }
}